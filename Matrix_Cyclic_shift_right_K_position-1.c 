/*Произвести циклический сдвиг столбцов квадратной матрицы A[N] на K позиций вправо.
-------
Входные данные:
Одно натуральное число N. Далее с новой строки N строк по N целых чисел в каждой.
N не превышают десяти. Затем с новой строки записано натуральное число K.
-------
Выходные данные:
Вывести на исходную матрицу N на N, столбцы которой циклически сдвинуты на
K позиций вправо. Числа разделять пробелами.
-------
3
1 2 3
1 4 5
2 3 -2
2
**********
2 3 1
4 5 1
3 -2 2  */

#include <stdio.h>

void printMatrix(int n, int data[n][n]) {
    for (int i = 0; i < n; i++) {
        for (int j = 0; j < n; j++) {
            printf("%d ", data[i][j]);
        }
        printf("\n");
    }
}

int main() {
    int n = 0, k = 0;

    if ((scanf("%d", &n)) != 1 || n < 1) {
        printf("n\a");
    }
    int data[n][n];
    for (int i = 0; i < n; i++) {
        for (int j = 0; j < n; j++) scanf("%d", &data[i][j]);
    }

    scanf("%d", &k);

    for (int i = 0; i < n; i++) {
        for (int c = 0; c < k; c++) {  // этим циклом прокручиваем сдвиг вправо К - раз!!!
            for (int j = n - 1; j > 0; j--) {
                int tmp = data[i][j - 1];
                data[i][j - 1] = data[i][j];
                data[i][j] = tmp;
            }
        }
    }

    printMatrix(n, data);

    return 0;
}