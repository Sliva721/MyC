/* Функция считывания строки из потока ввода по отдельным словам
*/

    char str[100];
    while (scanf("%s", str) == 1) { //это функция чтения строк по отдельным словам
     int len = strlen(str); 
    }
      