#include <stdio.h>
#include <math.h>

int main() 
{
 int N;
    scanf("%d",&N);
    
 int k=1,a=0;
// printf("%d\n",N);
    for (;N<=1;printf "\n); 
    
     while (a < N) 
        a=pow(2,k);
          //  printf ("a=%d ",a); 
      printf ("%d ",k);
          k++;  
  
    return 0;
}