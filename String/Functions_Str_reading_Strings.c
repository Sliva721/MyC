/*Функция чтения строки из входного апотока до конца - символа '\0'
*/
while (fgets(str, sizeof(str), stdin)) {

}

/*Функция чтения строк по отдельным словам (до пробела)
*/
while (scanf("%s", str) == 1) {

}
/*Функция чтения символов (строк в т.ч.) до конца потока
*/
while ((c = getchar()) != EOF) {  // считываем по одному символу до конца файла - EOF

}
